// Scaffold from T1. T5 implements using @anthropic-ai/claude-agent-sdk.
//
// Offline-only. Runs the render prompt against a fixture corpus, then asks
// Claude (via the SDK with the user's Claude Code session auth) to judge each
// rendering against a rubric. Reports scores.
//
// Not part of the runtime render path — that's pure prompt + sanitizer.

throw new Error('bin/eval.ts not yet implemented — see T5');
